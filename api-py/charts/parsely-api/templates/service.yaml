apiVersion: v1
kind: Service
metadata:
  name: {{ include "parsely-api.fullname" . }}
  labels:
    {{- include "parsely-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "parsely-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
